/* 
    Create a program that will contain two variables that their values will contain
    one part of a sentence

    Print out the combination of both parts
*/

const hello = 'Hello';
const world = 'World';

// Option 1:
console.log(hello + ' ' + world);

// Option 2:
const sentence = hello + ' ' + world;
console.log(sentence);
