/*
    Create a program that consists of 1 variable:
    1. str - contains a value of 'fdfddf'

    Print out to the console str + 1 and see what happens.
    Print out to the console str * 2 and see what happens.
*/

const str = 'fdfdf';
console.log(str + 1);
console.log(str * 1);
