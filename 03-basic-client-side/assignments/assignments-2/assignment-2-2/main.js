const headerEl = document.getElementById('main-header');
const mainEl = document.getElementById('main-content');

headerEl.innerHTML = '<h1>Inner HTML Example</h1>';
mainEl.innerHTML = '<p>I was able to add text with JavaScript to the HTML page.</p>';
